# Frontend configuration
# The contact form no longer requires a backend or API URL; it uses a mailto link.
# If you add other features that communicate with a server, you can set the
# API base URL here.
# VITE_API_URL=http://localhost:5000
